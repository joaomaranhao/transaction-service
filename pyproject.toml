[project]
name = "dm-transaction-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.131.0",
    "loguru>=0.7.3",
    "sqlmodel>=0.0.37",
]
